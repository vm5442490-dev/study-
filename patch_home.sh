#!/bin/bash
sed -i '411,425d' app/src/main/java/com/example/ui/screens/HomeScreen.kt
sed -i '/\/\/ 3.5 JAC Class 12 Special Banner/i \
                // AdMob Banner (Between Daily Quiz and JAC Class 12)\n                item {\n                    Box(modifier = Modifier.fillMaxWidth().padding(vertical = 8.dp), contentAlignment = Alignment.Center) {\n                        AndroidView(\n                            factory = { context ->\n                                AdView(context).apply {\n                                    setAdSize(AdSize.BANNER)\n                                    // Real Ad Unit ID for production\n                                    adUnitId = "ca-app-pub-3665825190622425/5013536957"\n                                    loadAd(AdRequest.Builder().build())\n                                }\n                            }\n                        )\n                    }\n                }\n' app/src/main/java/com/example/ui/screens/HomeScreen.kt
